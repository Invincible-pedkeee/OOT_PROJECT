<Window x:Class="Muzej.addTWWindow1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Muzej"
        mc:Ignorable="d"
        Title="addTWWindow1" Height="450" Width="550" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Naziv tipa dela-->
        <TextBlock Text="Tip:" FontWeight="Bold" Grid.Row="0" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Margin="5" IsReadOnly="True" SelectedIndex="-1" x:Name="cmbTip">
            <!-- Tipovi koje mozemo da izaberemo-->
            <ComboBoxItem Content="Fotografija"/>
            <ComboBoxItem Content="Instalacija"/>
            <ComboBoxItem Content="Mozaik"/>
            <ComboBoxItem Content="NFT"/>
            <ComboBoxItem Content="Skulptura"/>
            <ComboBoxItem Content="Slika"/>
        </ComboBox>

        <!--Naziv dela-->
        <TextBlock Text="Naziv dela:" FontWeight="Bold" Grid.Row="1" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtNazivTW" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!--Ikonica-->
        <TextBlock Text="Ikonica:" FontWeight="Bold" Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1" Margin="5">
            <TextBox x:Name="txtIkonica" Width="250" Margin="0,0,5,0"/>
            <Button Content="..." Width="30" Click="BiranjeIkonice_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 100 0 0">
            <Button Content="Sačuvaj" Width="100" Height="50" Margin="5" Click="Sacuvaj_Click"/>
            <Button Content="Otkaži" Width="100" Height="50" Margin="5" Click="Otkazi_Click"/>
        </StackPanel>

    </Grid>
</Window>
