<Window x:Class="Muzej.addWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Muzej"
        mc:Ignorable="d"
        Title="addWindow" Height="450" Width="550" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Šifra -->
        <TextBlock Text="Šifra:" FontWeight="Bold" Grid.Row="0" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtSifra" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Naziv -->
        <TextBlock Text="Naziv:" FontWeight="Bold" Grid.Row="1" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtNaziv" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Autor -->
        <TextBlock Text="Autor:"  FontWeight="Bold" Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtAutor" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <!-- Godina nastanka -->
        <TextBlock Text="Godina nastanka:" FontWeight="Bold" Grid.Row="3" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtGodina" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <!-- Tip -->
        <TextBlock Text="Tip:" Grid.Row="4" FontWeight="Bold" Grid.Column="0" Margin="5" VerticalAlignment="Center" />
        <ComboBox x:Name="cmbTip" Grid.Row="4" Grid.Column="1" Margin="5" >
            <ComboBoxItem Content="Slika"/>
            <ComboBoxItem Content="Skulptura"/>
            <ComboBoxItem Content="Fotografija"/>
            <ComboBoxItem Content="Mozaik"/>
            <ComboBoxItem Content="NFT"/>
            <ComboBoxItem Content="Instalacija"/>
        </ComboBox>

        <!-- Ikonica -->
        <TextBlock Text="Ikonica:" FontWeight="Bold" Grid.Row="5" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.Column="1" Margin="5">
            <TextBox x:Name="txtIkonica" Width="250" Margin="0,0,5,0"/>
            <Button Content="..." Width="30" Click="OdaberiIkonicu_Click"/>
        </StackPanel>

        <!-- Dugmad -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 100 0 0">
            <Button Content="Sačuvaj" Width="100" Height="50" Margin="5" Click="Sacuvaj_Click"/>
            <Button Content="Otkaži" Width="100" Height="50" Margin="5" Click="Otkazi_Click"/>
        </StackPanel>
    </Grid>
</Window>
